# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Rust (for the v0 maturin core)
/target/
# Cargo.lock IS committed — ridgepoint ships a binary/app, so we pin deps for reproducibility.

# OS / editor
.DS_Store

# Internal design / reflection notes — LOCAL ONLY, never pushed.
# We ship logic (code), not our thinking. README.md at root stays tracked.
design/
